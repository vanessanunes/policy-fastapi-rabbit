[project]
name = "policy-fastapi-rabbit"
version = "0.1.0"
description = ""
authors = [
    {name = "Vanessa Nunes",email = "dev.vanessanunes@hotmail.com"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[all] (>=0.136.3,<0.137.0)",
    "pydantic-settings (>=2.14.1,<3.0.0)",
    "uvicorn (>=0.49.0,<0.50.0)",
    "pymongo (>=4.17.0,<5.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
